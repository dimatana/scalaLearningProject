openapi: 3.0.3
info:
  title: Ping Service API
  description: Toy API to learn the contract-first workflow (openapi-generator).
  version: 1.0.0

paths:
  /ping:
    get:
      operationId: ping
      summary: Health check endpoint
      tags:
        - Ping
      responses:
        '200':
          description: Service is alive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PingResponse'

components:
  schemas:
    PingResponse:
      type: object
      required: [status, timestamp]
      properties:
        status:
          type: string
          example: "pong"
        timestamp:
          type: string
          format: date-time